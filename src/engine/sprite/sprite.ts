export class Sprite {
    width: number;
    height: number;

    constructor(width: number, height: number) {
        this.width = width;
        this.height = height;
    }

    update(dt: number) {
        // Update logic here
    }

    draw(x: number, y: number) {
        // Draw logic here
    }
}
